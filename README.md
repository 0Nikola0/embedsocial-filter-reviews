# Solution for EmbedSocial's Filter reviews challenge

- Installation
```
python -m pip install -r requirements.txt
```

- Run
```
python manage.py runserver
```

The webpage will be running on localhost after this

